namespace S_H2
{
    // Handles the convertion of messages to HTML/Plaintext
    public class ConvertText
    {
        public string ConvertBodyToText(string plainText)
        {
            return "" + plainText + "";
        }
    }
}